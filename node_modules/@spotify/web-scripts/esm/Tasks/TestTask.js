import { default as spawn } from 'cross-spawn';
import { default as Debug } from 'debug';
const dbg = Debug('web-scripts:test');
export function testTask(task) {
    const cmd = 'npx';
    const args = [
        '--no-install',
        'jest',
        '--config',
        task.config,
        ...task.restOptions,
    ];
    dbg('npx args %o', args);
    return spawn.sync(cmd, args, { stdio: 'inherit' });
}
//# sourceMappingURL=TestTask.js.map